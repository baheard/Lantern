---
name: build-scenes
description: RETIRED — use /studio instead.
---

# build-scenes — retired

Replaced by `/studio`. Goals 2 + 3 in the studio menu (image prompts → scene overrides) cover what this skill did. Use `/studio <game>` to see where you are in the pipeline and what to run next.
