---
name: mold
description: RETIRED — split into /frame (judgment) + /scene (prose). Use those, or /studio to orchestrate.
---

# mold — retired

Phase 2 of the location-art pipeline was split into two focused skills (2026-06-29):

- **`/frame <game>`** — facts → `location-framing.md` (the JUDGMENT: vantage, occlusion, exits, state,
  shared-volume geometry, the 12-factor checklist). Pin the camera facing here.
- **`/scene <game>`** — `location-framing.md` → `style.json` scenes (the PROSE: REDACT + RE-ANCHOR,
  compass→frame translation, the compass/vague-placement lint, layer discipline).

Run `/frame` then `/scene` (or let **`/studio <game>`** orchestrate). The pipeline step is still
stamped `mold` by `/scene` on completion, so `/studio` staleness detection is unchanged.

This stub can be deleted once muscle memory has moved on.
